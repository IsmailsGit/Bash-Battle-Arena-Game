Level 1 completed - I used the mkdir command to make the directory 'Arena'. I then went into that directory using the command cd Arena.
Then I used the touch command to make the files warrior.txt, mage.txt, and archer.txt.

